"""Machine-learning subsystem: feature engineering, dataset construction, and
the model benchmark (temporal evaluation, tuning, calibration).

The runtime scorer lives in ``ar_platform.tools.ml_risk`` and consumes the
artifacts produced here.
"""
